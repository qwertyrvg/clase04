class Boton {
    constructor(display, color, padding, text-decoration) {
        this._display = display,
            this.color = color,
            this.text-decoration = text-decoration;
    }
}

let boton1 = new Boton

class de Video {
    constructor(description, width, height) {
            this.description = description,
            this.width = width,
            this.height = height,
    }
}

class de Audio {
    constructor(title, url) {
        this.title = title,
            this.url = url,
    }
}

class de List {
    constructor(elements, styles, className) {
        this.className = className,
        this.elements = elements,
    }
}


